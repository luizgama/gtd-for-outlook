# Supply chain security: disable postinstall scripts by default
ignore-scripts=true

# Package cooldown: avoid packages published within the last 7 days
# Uncomment when supported by your npm version:
# min-release-age=7

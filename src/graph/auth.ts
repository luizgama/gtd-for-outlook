// TODO: MSAL OAuth2 + persistent token cache
// - Device code flow for first-time authentication
// - Token cache persistence to ~/.gtd-outlook/token-cache.json
// - Silent token acquisition for subsequent runs
// - Automatic token refresh via refresh token

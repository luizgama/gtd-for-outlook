// TODO: gtd_classify_email tool implementation
// - Run full security pipeline (sanitize → detect → classify → validate)
// - Content-hash dedup check before LLM calls
// - Metadata triage check before LLM calls
// - Return classification result
